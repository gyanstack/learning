export interface DialogInfo {
    control: any;
    /** Width of the dialog. */
    width?: string;
    /** Height of the dialog. */
    height?: string;
    /** Data being injected into the child component. */
    data?: any;
}

